version: "1"
build:
  environment:
    PYTHON_VERSION: 3.10

services:
  - type: web
    name: discord-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: TOKEN
        sync: false